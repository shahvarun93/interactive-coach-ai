apiVersion: apps/v1
kind: Deployment
metadata:
  name: sd-copilot-deployment-uat
  namespace: sd-copilot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sd-copilot-uat
  template:
    metadata:
      labels:
        app: sd-copilot-uat
    spec:
      containers:
        - name: sd-copilot-uat
          image: sd-copilot-uat:placeholder   # still using Option A, we override via kubectl set image
          ports:
            - containerPort: 4000
          envFrom:
            - secretRef:
                name: sd-copilot-secrets-uat
          env:
            - name: NODE_ENV
              value: "uat"
            - name: PORT
              value: "4000"
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 4000
            initialDelaySeconds: 15
            periodSeconds: 20
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 4000
            initialDelaySeconds: 15
            periodSeconds: 20